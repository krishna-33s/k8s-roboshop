apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: roboshop
  # this labels is for replicaset
  labels:
    project: roboshop
    tier: database
    component: redis
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      # these labels is for replicaset to select the pods
      project: roboshop
      tier: database
      component: redis
      environment: dev
  template:
    metadata:
    # these for pod defination
      labels:
        project: roboshop
        tier: database
        component: redis
        environment: dev
    spec:
      containers:
      - name: redis
        image: krishna88s/redis:1.1.0
        imagePullPolicy: Always
        readinessProbe:
          tcpSocket:
            port: 6379
          failureThreshold: 4
          periodSeconds: 10 


---

apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: roboshop
spec:
  selector:
      project: roboshop
      tier: database
      component: redis
      environment: dev

  ports:
  - protocol: TCP
    port: 6379
    targetPort: 6379

